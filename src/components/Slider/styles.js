import styled from "styled-components";

export const Container = styled.div`
    padding: 0 20px;
    background: #000;
    overflow: hidden;
    .swiper-wrapper {
        display: flex;
    }

    h2{
        font-size: 24px;
        font-weight: 500;
        color: #fff;
        margin: 50px 0 20px 20px;
    }
`;